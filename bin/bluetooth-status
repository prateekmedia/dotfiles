#!/bin/sh
if [ $(bluetoothctl show | grep -F "Powered: yes" | wc -c) -eq 0 ]
then
  echo "%{F#6B6B6B}"
else
  if [ $(echo info | bluetoothctl | grep -F 'Device' | wc -c) -eq 0 ]
  then 
    echo ""
  else 
    echo "%{F#0066CC}"
  fi
fi
